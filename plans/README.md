# Implementation Plans

Generated by the improve skill on 2026-06-11 against commit `105eacc` (standard-depth audit: all nine categories; subagent findings re-verified against the code before planning). The session ran non-interactively, so the top findings by leverage were planned by default — the maintainer should review this selection and may reject any plan by marking it REJECTED below.

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done. Test command for everything: `/mnt/c/Python313/python.exe -m pytest tests/ -q` (the WSL `python3` has no deps).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
| ---- | ----- | -------- | ------ | ---------- | ------ |
| 001 | Fix total_emissions_kgco2e to sum the per-tick impact series | P1 | S | — | DONE (merged into `main` as `219d81d`; red→green pinning test verified; reviewed and approved 2026-06-11 — outputs/ emissions numbers grow by orders of magnitude, re-run baseline before citing) |
| 005 | Declare scipy/matplotlib/pytest deps; fix stale README paths | P1 | S | — | DONE (merged into `main` as `c07840e` + `0d50035` via merge commit `5dbde81`; reviewed and approved 2026-06-11) |
| 003 | Characterization tests: generator overflow + cross-region double-count guard | P1 | M | — | DONE (merged into `main` as `83860e0`; reviewed and approved 2026-06-11) |
| 002 | Seed grid-mode runs for reproducibility | P2 | S | — | DONE (merged into `main` as `a522537` cherry-pick of `536aa17`; full suite 258 passed + 6 skipped re-verified post-merge; reviewed and approved 2026-06-11) |
| 004 | Cap per-type generation at storage headroom (was all-or-nothing drop) | P2 | S | 003 | DONE (merged into `main` as `e08cbf9`; reviewed and approved 2026-06-11 — outputs/ now stale, re-run baseline before citing numbers) |
| 006 | Regenerate the paper's Fig. 2 in the baseline flow | P3 | S | — (best after 001) | DONE (merged into `main` as `87e76ad`; behavioral check: figure regenerated against Baseline summary.csv files, valid PDF written; reviewed and approved 2026-06-11) |
| 007 | Parallelize Monte Carlo baseline replications across worker processes | P2 | M | — | IN PROGRESS (planned 2026-06-11 at `8592fe4`, promoted from the backlog; executor dispatched) |

All six plans are merged into `main` as of 2026-06-11 (final state: `a522537`). The plan-execution worktrees and branches have been removed. Reminder: `outputs/` predates plans 001/004 — re-run `python main.py --mode baseline --replications 100` before citing any number; the wired Fig. 2 will regenerate automatically.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 004 requires 003: plan 003 pins the current generator semantics; plan 004 deliberately flips one of those tests (the red→green flip documents the semantic change). If 004 somehow runs first, 003's executor pins the new capped behavior instead — its STOP conditions cover this.
- 006 is independent but only pays off fully after 001: the figure plots `total_emissions_kgco2e`, which 001 corrects.
- **After 001 and 004 land, all `outputs/` artifacts are stale.** The maintainer must re-run `python main.py --mode baseline --replications 100` before citing any number; the emissions KPI grows by orders of magnitude (001) and collection-rate/holding-cost KPIs shift (004). Both plans say so in their maintenance notes — this is a feature of fixing real bugs, not a regression.

## Audited but not planned (backlog — worth a future plan if the maintainer wants it)

- ~~**Parallelize Monte Carlo replications**~~ — promoted to plan 007 on 2026-06-11.
- **Raw-history JSON writes block the replication loop** (`main.py` writes `raw_NNN.json` via full `jsonify` deep-copy inline). Either offload to a writer thread or add a `--save-raw` opt-out. Verify first whether any analysis path consumes the raw sidecars.
- **Shared KanbanManager signal bus — investigate, don't fix**: one `KanbanManager` serves all entities; collectors read all non-market signals system-wide, and a collector that cannot serve a generator signal still acknowledges (consumes) it. Cross-region fulfillment is explicitly intended (docstrings), so this looks by-design, but the ack-by-non-serving-collector path could starve PULL signal chains. Worth a focused trace before any paper claim leans on PULL signal latency.
- **`OperationalEntity._failure_counts` is a class-level mutable shared across instances**, reset only in `SimulationManager.__init__` (`models/data_classes.py:18`). Affects only the diagnostic failure printout, but makes tests order-sensitive if they construct entities directly. S fix (instance variable).
- **Expansion-event KPI accounting in `core/collector.py:311-336`**: the two-phase overflow handler records the pre-expansion overflow amount on the expand branch while force-landfilling a different residual. Mass balance is intact; the `expansion_volume_m3` event bookkeeping may be inconsistent. MED confidence — verify by tracing one expand event before planning a fix.
- **Collector god object** (`core/collector.py`, 799 lines, 30 methods): extract the vehicle-dispatch concern into a `CollectorTransport` helper as a first slice. L effort; conflicts with the surgical-changes convention without explicit maintainer buy-in.
- **More test darkness**: collector dispatch chain (`_process_kanban_signals`, `collect_from_generator`), `main.py` seams (`_write_combo_summary`, the warn-and-continue batch loop), and `visualization/temporal_comparison.py` (703 lines, zero tests; also check whether the grid-mode-only `ScenarioComparison` path is still a publication path at all).
- **Rename `tests/monitoring/` → `tests/analysis/`** (the `monitoring/` source package it mirrored was deleted). S, cosmetic.
- **DX scaffolding**: `pyproject.toml` with `[tool.pytest.ini_options]`, ruff config, a 20-line GitHub Actions CI (needs plan 005's requirements fix first), optionally a lockfile.

## Direction findings (research options — maintainer's call, not defaulted into plans)

These are paper/PhD-level options grounded in repo evidence; each would become a design/spike plan on request:

1. **Run the stress scenarios into the paper**: SupplyDisruption and DemandSurge are fully wired and calibration-anchored (`config/base_config.py:108-129`) but `paper-draft/05-results.md` has no cross-scenario robustness section. Two `--mode baseline --scenario <X>` runs plus a §5.6 answer the inevitable reviewer question about robustness of the PUSH/PULL ordering.
2. **Surface holding cost** (ADR 0017): the first deliberately policy-sensitive cost component has no figure (`kpi_family_figures.py` lacks it) and no paper presence. One metrics-list line + a §5.2 paragraph with the placeholder-rate caveat.
3. **Sensitivity analysis the paper already promises** (`paper-draft/todo.md:267`): start with avoided-emissions factor bounds (pure post-hoc arithmetic over existing `run_*.json`, ADR 0011 names the four source papers); carbon-price and holding-cost sweeps need re-runs.
4. **Resolve the "Lebanon paper" landfill-cost citation** (`config/base_config.py:26`, flagged red in `paper-draft/todo.md:139`) — swap to the citable CEWEP/EEA figure already named in the currency-consistency issue; full EUR restatement stays post-paper per ROADMAP.
5. **Bullwhip metric redesign** (`.scratch/bullwhip/issues/13`): downstream-throughput anchoring + bin-width sweep. A PhD-chapter-scale spike superseding ADR 0004, not a current-paper item.

## Findings considered and rejected

- **Cross-region "requested counts as satisfied" timing** (treatment deducts `remaining` when transport departs, not on arrival): the docstring at `core/treatment.py:560` documents exactly this semantics as an ADR 0009 design choice. Not a bug; at most a paper-limitations sentence.
- **`analysis/pareto.py` lazy visualization import as a layering violation**: the lazy `try/except` import inside the CLI block is the standard cycle-breaker and degrades gracefully by design (comment says so). Not worth doing.
- **`current_storage` property recomputing a dict sum on the monitor path**: micro-optimization; a ~14-key dict sum a few times per tick is noise next to simulation cost. Not worth doing.
- **Stochastic-dominance FSD/SSD flag logic**: audited twice on suspicion of sign errors; the interleaved flag updates are correct (the auditor's own trace confirmed both orders). No change needed — though the non-obvious `if/elif` interleaving would merit a comment if the file is ever touched anyway.
- **Little's Law flat-back extrapolation bias**: first monitor sample lands at t=0, so the suspected zero-to-first-sample bias has zero width. No bug.
- **Pinning upper bounds / lockfile as a "must"**: this is a solo research repo with one known-good interpreter; floors + declared deps (plan 005) cover the real failure. Lockfile stays optional backlog.
